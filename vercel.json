{
  "buildCommand": "cd oscar-admin && npm ci && npm run build",
  "outputDirectory": "oscar-admin/dist",
  "installCommand": "echo skip -- the build command installs inside oscar-admin",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
